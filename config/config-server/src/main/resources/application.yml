server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KhuatKien/GraduationProject_Service_Repo.git
          default-label: main
          search-paths:
            - auth-service
            - user-service
            - tour-service
logging:
  level:
    org.springframework.cloud.config: DEBUG